using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Orby
{
    public class ThirdPhaseAttacks : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
